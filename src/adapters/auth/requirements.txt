# HR AI Platform - Core Dependencies

# Web Framework
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.0
python-multipart>=0.0.6  # Required for form data parsing

# Authentication
PyJWT>=2.8.0
cryptography>=41.0.0

# HTTP Client (for OIDC discovery)
httpx>=0.26.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.23.0

# YAML processing (for policy)
pyyaml>=6.0.0
