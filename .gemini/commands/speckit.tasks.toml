description = "Break the implementation plan into actionable tasks"
instruction = "Generate a granular task list from the implementation plan"

[arguments]
feature_name = { type = "string", description = "Name of the feature to generate tasks for (e.g. 004-workday-actions)" }